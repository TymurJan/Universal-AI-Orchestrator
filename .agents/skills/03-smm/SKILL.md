---
name: smm-automation
description: Універсальний SMM-Термінал. Дозволяє агенту самостійно відправляти тексти та зображення у будь-який Telegram канал через офіційне API.
capability_id: social-media-publisher
domain: smm
version: 1.0.0
synced_function: smm_poster.py
---

# SMM Automation Skill

Цей скіл перетворює Агента на автономного SMM-менеджера. Агент більше не залишає фінальні тексти просто в чаті — після затвердження тексту користувачем, Агент може **самостійно його опублікувати** в соцмережі.

## 🚀 Протокол Автопостингу

Коли користувач затвердив текст і дає команду "Публікуй" / "Відправ", Агент **ЗОБОВ'ЯЗАНИЙ**:

1. Запитати або дістати з локальних налаштувань проєкту (якщо вони були надані раніше) "Telegram Bot Token" та "Chat ID".
2. Зберегти фінальний текст (і, якщо є, фото) локально або передати їх безпосередньо у скрипт.
3. Викликати системну команду:

```powershell
python Universal-AI-Orchestrator/tools/smm_poster.py --token "ВАШ_ТОКЕН" --chat "@ваш_канал" --text "Текст публікації"
```

Якщо до публікації додається графік (наприклад, згенерований `data-visualizer`):
```powershell
python Universal-AI-Orchestrator/tools/smm_poster.py --token "ВАШ_ТОКЕН" --chat "@ваш_канал" --text "Аналітика" --photo "Universal-AI-Orchestrator/REPORTS/graphs/budget.png"
```

### Важливі правила безпеки:
- Ніколи **не відправляти** пост без фінального підтвердження тексту користувачем.
- У тексті дозволено використання HTML-тегів Telegram (наприклад, `<b>жирний</b>` або `<i>курсив</i>`).
